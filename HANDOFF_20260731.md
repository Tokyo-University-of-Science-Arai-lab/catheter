# 引き継ぎメモ（2026-07-31）

`~/pro_book` での作業を `~/pro_book_SAM3` に移行した時点の記録。
新しいセッションはこれを読めば経緯を把握できる。

---

## 1. 何をしていたか

**カテーテル箱に対する SAM3 認識の精度をオフラインで評価していた。**

- 対象: カテーテル20種（`master_20260216.json`）
- 手法: 棚に20種を並べて5枚撮影 → 各画像に対し20種すべてを query として認識
- 評価数: 5枚 × 20種 = **100試行**
- 実行: `python offline_pointcloud_debug_SAM3.py`

### なぜ5枚で100試行なのか

1枚の画像に全20種が写っている。認識は `query`（型番）を受け取って
「その画像の中から該当する箱を探す」仕組みなので、
**同じ画像を20回、違う query で処理すれば20評価が得られる**。
撮影は5回で済む。

この構造に合わせて `offline_pointcloud_debug_SAM3.py` を書き換えた（後述）。

---

## 2. 評価結果（旧環境 `~/pro_book` での実測）

結果の場所: `captures/5shot_catheter_offline/20260731_124927/`

### 成功率は定義によって大きく変わる

| 水準 | 値 | 意味 |
|---|---|---|
| 処理が完走した割合 | 100% | 例外が出なかっただけ。**精度ではない** |
| 正しい箱を選べた割合 | **70〜82%** | 確実な下限70%、判定不能22件を全て正解と仮定して上限82% |
| 幅の誤差2mm未満 | **28%** | ただし下記の理由で水増しされている |

| しきい値 | 割合 |
|---|---|
| 1.0mm未満 | 15% |
| 2.0mm未満 | 28% |
| 3.0mm未満 | 43% |

**参考**: 先輩が同じコードを**書籍**で評価したときは 2mm以内 **83%**、最大誤差 3.79mm。
軸反転は一度も起きていない。コード自体は書籍では正常に動く。
カテーテル固有の事情で性能が落ちている。

---

## 3. 見つかった3つの不具合（未修正）

### ① 箱の取り違え（100試行中8件が確定、12件が疑い）

**正解の箱を検出できているのに、照合スコアで無関係な箱に負ける。**

例: CHIKAI（型番 `AIN-CHI-200R`）は5回中5回とも失敗。

| マスク | OCRが読んだ文字 | スコア | 結果 |
|---|---|---|---|
| mask_19 | `TransForm 4 REF ESC0407 ...` | 50 | ← 選ばれた（無関係） |
| mask_16 | `ASAHI INTECC **CHIKAI** 0.014"X200cm` | 41 | ← **本物なのに落選** |

原因: 型番 `AIN-CHI-200R` と箱の印字 `CHIKAI` の字面が違うため照合が伸びない。
閾値40をかろうじて超えた無関係な箱が勝つ。

**繰り返し失敗する品目**: CHIKAI(5/5)、Strykerの厚いオレンジの箱(3/5)。
**判定不能の品目**（型番も商品名もOCRで読めない）: pNOVUS 17 / Phenom 160cm /
Wallaby Avenir / Esperance（各5/5）。

### ② 軸の反転（100試行中26件）

**幅を測る向きが90°ずれ、棚全体の長さを測ってしまう。** 推定幅が78〜270mmになる。

原因は [detection/pro_handbook/sam_py_demo/get_book_points.py:67-69](detection/pro_handbook/sam_py_demo/get_book_points.py#L67-L69):

```python
if a in b or b in a:
    return 1.0
```

**文字数の下限が無い。** 型番 `ESC0407` を探すとき、箱に印字された `4` の1文字が
「`esc0407` に含まれる」ので類似度**満点1.000**になる。

| OCRテキスト | 類似度 |
|---|---|
| `ESC0407` | 1.000 |
| `4` | 1.000 |
| `7` | 1.000 |
| `40` | 1.000 |

幅を測る軸は「型番の文字が並ぶ向き」から決めるが、1文字の輪郭はほぼ正方形で
向きが定まらない。結果、軸が90°倒れる。

**証拠**: 軸が反転した26件で軸の基準に選ばれた文字は平均1.1文字、
**全26件が3文字以下**（`'0'` `'1'` `'2'` `'3'` `'4'` など）。
正常だった74件は平均5.4文字。

マスク自体は正しい（短辺35〜38px = 箱の厚み）のに、長辺502〜554pxを測っていた。

### ③ 偽りの好成績（①の副作用）

別の箱を測って偶然近い値が出る。例: CHIKAI shot1 は誤差0.5mmだが、
実際には TransForm の箱を測っていた。**この件は「2mm未満28%」の分子に入っている。**

過大推定73件 vs 過小推定27件と偏っており、軸ずれの影響が中間層にも及んでいることを示唆。

### 修正の見通し

`_text_similarity` に最低文字数の条件を足すのが②の対策。
確認済みの事実:
- この関数が実際の判断に使われるのは軸選択の1箇所のみ
  （他2箇所 5663行・8207行はデバッグ画像の描画用）
- 箱の選択は `match_text_to_mask_main` という**別の関数**なので影響しない
- したがって**実機の把持動作には影響しない**

試算: 同じマスクのまま短辺を測っていれば 2mm未満は 28% → **69%** になる
（マスクPCAによる粗い近似）。

①は別の原因（照合スコア）なので、別途対策が必要。
`display_name` も照合対象に加える、閾値を上げる、などが候補。

---

## 4. 今日作ったスクリプト

| ファイル | 用途 |
|---|---|
| `capture_5shot.py` | 評価用に棚を撮り直す。Enterごとに1枚、間に配置を変える |
| `summarize_eval_csv.py` | 各shotに「書籍名・推定幅・誤差」のCSVを作る |
| `export_results_xlsx.py` | `results.csv` を Excel 化（色分け・オートフィルタ付き） |
| `collect_final_images.py` | `final.png` を display_name 名で1箇所に集める（目視確認用） |

`offline_pointcloud_debug_SAM3.py` は大幅に書き換えた:
- 1重ループ（画像1枚=1試行）→ **2重ループ（画像 × 全品目）**
- 出力を `shot<N>/<品目番号>_<型番>/` に分離（同じ画像を複数queryで処理するため）
- `--shots 1,3` `--book-index 5` で部分実行が可能
- 品目ごとの集計（平均誤差・最大誤差・ばらつき）を追加
- `BASE_DIR` を `Path(__file__).resolve().parent` に変更（場所非依存）

`capture_burst.py` に `--save-root` `--n-total` `--batch-size` を追加。

---

## 5. 出力の見方

```
captures/5shot_catheter_offline/<timestamp>/
├── results.csv / results.xlsx / summary.json
└── shot1〜5/
    ├── recognition_summary.csv     ← 書籍名・推定幅・誤差
    ├── final_images/               ← display_name.png（目視確認用）
    └── 00_ESC0407/ ... 19_ESC0305/
        ├── final.png                        選ばれた箱が緑、把持点が赤
        ├── ocr_axis_overlay.png             軸（マゼンタ線）→ 軸反転の確認用
        ├── selected_mask_refined_overlay.png マスク重ね
        ├── similarity_scores.json           マスクごとのOCR文字と照合スコア
        └── sam3_refined_sam2_width_result.json  width_info に pixel_width と axis
```

軸反転を確認するなら `sam3_refined_sam2_width_result.json` の
`width_info.pixel_width`（測った長さ）と `axis`（向き）を見る。

---

## 6. 環境について

### GPUは正常に使えている

「GPUが使えていないから遅いのでは」という疑いがあったが、**使えている**。

```
SAM3サービス: GPU 3978 MiB 使用 / ウォームアップ推論 0.73秒
OCR: paddle.device=gpu:0
```

**10秒/試行の原因は重複計算。**

```
SAM3で全マスク生成  ← queryに依存しない
OCRで全文字を読む   ← queryに依存しない
照合してマスク選択  ← ここだけqueryに依存
```

前半2つは画像だけで決まるのに、同じ画像に対し20回繰り返している。
OCRだけで毎回3.6秒（モデル生成1.7 + 推論1.5 + 保存0.4）。
本来は画像5枚につき5回で済むので、**理論上5分の1に短縮できる**。
未着手。

### 移行で分かった落とし穴

`~/pro_book_SAM3/RESTORE.md` に全て記載済み。特に:

1. **クローン元は `catheter` リポジトリ**（`book` ではない）。
   `book` には研究室独自の20コミットが無い。
2. **paddle は cu129 でないとダメ。** cu126 だと**エラーを出さずにOCRが0件検出**になる。
3. **venv は `--system-site-packages` 必須**（ROSのrclpyを借りている）。
4. `pip freeze` ではなく `pip list --local` を使う（システムパッケージが混ざる）。
5. `vendor/owner_repo/config/local_paths.json` に旧パスが記録されている。書き換え必要。
6. `sam3_runtime/logs/` ディレクトリが無いとサービス起動が失敗する。
7. `captures/100test/1/after_init_rgb.png` がウォームアップに必要。

### 実行中のもの

移行直後に新環境で100試行を実行した（`--external-service` 付き）。
結果は `~/pro_book_SAM3/pro_hand_book_python/captures/5shot_catheter_offline/` の最新。
**旧環境の `20260731_124927` と一致すれば移行が正しいことの確認になる。**

---

## 7. 次にやること（候補）

1. **新旧の100試行結果を照合** — 移行の正当性確認（実行済み、結果待ち）
2. **①取り違えの対策** — 実機で違う商品を掴む危険があるので優先度が高い
3. **②軸反転の修正** — `_text_similarity` に最低文字数。影響範囲は確認済み
4. **重複計算の除去** — 評価時間を1/5に
5. 修理前のバックアップ（`RESTORE.md` 参照。モデル5.6GB、`vendor/`、`handeye_pairs/` は
   GitHubに無いので特に重要）

---

## 8. 注意

- **数値をそのまま外部に出さない方針。** 28%は3つの不具合の合成値で、
  偽りの好成績を含むため、SAM3の性能を表していない。
- `~/pro_book` は参考用に残してある。編集は `~/pro_book_SAM3` 側で行う。
- 先輩PC（`ssh senpai` = 192.168.1.44）のファイルは**読み取りのみ。絶対に消さない。**
